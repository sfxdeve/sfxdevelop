---
interface Props {
  label?: string;
}

const { label } = Astro.props;
---

<div class="flex items-center gap-3">
  <span class="h-1 w-full bg-paper-50"></span>
  {label && <span class="font-mono text-[0.65rem] uppercase tracking-[0.16em] text-paper-100">{label}</span>}
</div>
